name: pyari_desh
description: Collection of countries from Api

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.0
  connectivity_plus: ^1.0.6
  flutter_riverpod: ^0.14.0+3
  flutter_staggered_grid_view: ^0.4.0
  country_icons: ^2.0.2


  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/internet.png
